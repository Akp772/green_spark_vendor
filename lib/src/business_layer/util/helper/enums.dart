enum ForgotScreenEnum { enterEmail, verifyEmail, resetPassword }

enum OnBoardingScreensEnums {
  languageScreen,
  accountType,
  dietType,
  subscriptionScreen
}

enum OnBoardingLanguageEnum { english, norwegian }

enum DiscoverFollowEnums { discover, follow }

enum WebViewScreen { aboutUs, terms, privacy }

enum RecipeEnum { recipe, masterClass }

enum RecipeStepEnum { steps, ingredient }

enum SocialLoginType { google, facebook, apple }

enum SearchEnum { recipe, masterClass, people }
